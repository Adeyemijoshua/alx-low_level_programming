#include "main.h"

/*
 * _isupper - checks uppercase
 * @c: parameter
 * Description:to check for upper and lower case
 * Return: 0 if sucessful
 */

int _isupper(int c)
{
if (c >= 65 && c < 91)
{
	return (1);
}
else
{
	return (0);
}
}
